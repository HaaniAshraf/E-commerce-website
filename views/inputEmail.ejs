<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Input Email</title>
    <link rel="stylesheet" href="/css/mainCSS/otp.css" />
  </head>
  <body>
    <style>
      label {
        margin-top: 30px;
        text-align: left;
        width: 78%;
      }
      input {
        margin: 0;
        margin-top: 5px;
        font-size: 15px;
      }
    </style>

    <form
      action="/inputEmail"
      method="post"
      id="otpForm"
      class="otpForm"
      style="height: 230px"
    >
      <div class="left">
        <img class="logo" src="images/cartlogo.png" alt="" />
        <div class="SHOPON">
          <span class="shop">&nbsp;SHOP</span>
          <span class="on">ON</span>
        </div>
      </div>
      <label for="">Enter your Email :</label>
      <input
        type="text"
        placeholder="abc@gmail.com"
        id="email"
        name="email"
        required
      />

      <% if (errors) { %>
      <p style="color: rgb(235, 80, 80); margin-top: 4px"><%= errors %></p>
      <% } %>

      <button type="submit" class="verify">Send OTP</button>
    </form>
  </body>
</html>
